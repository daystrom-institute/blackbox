{
  "_contract": "atom/v1",
  "kind": "atom",
  "name": "java-extract-cohesive-class",
  "version": 3,
  "subcontract": "refactor/v1",
  "manifest": {
    "description": "Extract a cohesive Java cluster — methods + field moves + delegate wiring + caller delegation + accessor rewrites + cross-package widening — into a new class via the composite extract_java_class plan kind.",
    "when_to_use": [
      "a class has a tight cluster of methods + fields that belong together (e.g. one grid + its data provider + the methods that build them)",
      "splitting a Vaadin view or service class along a clear functional seam",
      "the operator has identified the methods and fields and wants the composite plan to handle delegate wiring + caller rewrites + visibility + import organization in one pass"
    ],
    "anti_patterns": [
      "do not use when the cluster includes a non-static inner class with outer captures — chain java-promote-inner-class first",
      "do not use to lift a method subset onto a NEW interface — use java-extract-interface for the interface shape",
      "do not use on non-Java files",
      "do not pass an inner-class name in item_names — extract_java_class hard-refuses with nested_class_in_item_names"
    ],
    "inputs": {
      "schema": {
        "type": "object",
        "required": [
          "project_dir",
          "source",
          "target",
          "module_name",
          "item_names",
          "delegate_field"
        ],
        "properties": {
          "project_dir": {
            "type": "string"
          },
          "source": {
            "type": "string"
          },
          "target": {
            "type": "string"
          },
          "module_name": {
            "type": "string",
            "description": "Target class name. extract_java_class derives the file class wrapper from this when the target file is missing."
          },
          "item_names": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "string"
            },
            "description": "Method names to move. Pass `methodName(Type1, Type2)` to disambiguate overloads."
          },
          "move_fields": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Instance fields to move with the methods. Triggers accessor generation + remaining-source rewrites by default."
          },
          "delegate_field": {
            "type": "string",
            "description": "Name of the delegate field added to the source class for the new target."
          },
          "target_prelude": {
            "type": "string",
            "description": "Explicit package + import header for the target file. Overrides path-derived package resolution."
          },
          "rewrite_remaining_accessors": {
            "type": "boolean",
            "description": "Defaults to true when move_fields is non-empty. Pass false to opt out of source-side delegate.getX()/.setX() rewrites + target accessor generation."
          },
          "callback_externals": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Source-class methods to thread as functional-interface callbacks (Runnable / Supplier / Consumer / Function) instead of leaving FIXME markers. Two-plus-arg methods refuse with callback_arity_unsupported."
          },
          "visibility": {
            "enum": [
              "package",
              "public"
            ],
            "description": "Floor for moved-method visibility. Defaults to package for same-package extracts, public for cross-package."
          },
          "wiring_strategy": {
            "enum": [
              "own_construction",
              "external_injection",
              "none"
            ],
            "description": "How the source class obtains the new delegate field. `own_construction` (default) synthesizes `private final <Target> <delegate>;` + `this.<delegate> = new <Target>(...)` in the source's first constructor. `external_injection` field-injects the delegate (`@Inject private <Target> <delegate>;`, no ctor wiring; the generated target constructor is also `@Inject`-annotated with the `javax.inject.Inject` import) for DI-managed (Guice/JSR-330) sources. `none` skips source-side wiring entirely. The engine does NOT auto-detect DI: if the source has `@Inject` fields you MUST pass `external_injection` or `none` — `own_construction` would null-capture the delegate."
          },
          "source_delegate_wrappers": {
            "type": "boolean",
            "description": "When true, generate thin wrapper methods on the source for each moved public non-static method (signature + throws preserved, body delegates through the new field). Makes public-method moves source-API-compatible. Static methods are auto-qualified by class instead — not wrapped. Default false."
          },
          "propagate_class_annotations": {
            "type": "string",
            "description": "Controls whether class-level annotations on the source propagate to the target. `auto` (default) propagates when moved bodies reference a known annotation-processor-generated identifier (current heuristic: `@Slf4j` → `log`). `all` copies every annotation. `none` strips all. `list:@A,@B` is an operator-supplied allowlist."
          },
          "apply": {
            "type": "boolean"
          },
          "validation_cmd": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Validation command + args. Defaults to [\"mvn\", \"compile\"] when omitted."
          }
        }
      },
      "prompt_template": "You are java-extract-cohesive-class. Charter: Extract a cohesive Java cluster into a new class via the composite extract_java_class plan.\n\nProtocol:\n\n1. Ground the target structurally:\n   bbox_code_symbols(project_dir=\"{{project_dir}}\", query=\"{{module_name}}\",\n                     languages=[\"java\"], item_kinds=[\"class_declaration\", \"method_declaration\"])\n   bbox_refactor_status(file=\"{{source}}\", project_dir=\"{{project_dir}}\")\n   Confirm every name in {{item_names}} resolves on the source class. For\n   overloaded methods, copy the exact `methodName(Type1, Type2)` signature\n   suffix from bbox_code_symbols — bare ambiguous names refuse with\n   method_overload_ambiguous.\n\n   wiring_strategy selects how the source obtains the delegate field. The\n   engine does NOT auto-detect DI — inspect the source first. If it has any\n   `@Inject`-annotated field (DI-managed), you MUST pass\n   `wiring_strategy=external_injection` (DI populates the field) or\n   `wiring_strategy=none` (operator wires by hand) — NEVER `own_construction`,\n   which would null-capture the delegate on a DI-managed class.\n\n2. Plan with deep_analysis=true (REQUIRED for atoms):\n   bbox_refactor_plan(kind=\"extract_java_class\", deep_analysis=true,\n                      project_dir=\"{{project_dir}}\",\n                      source=\"{{source}}\", target=\"{{target}}\",\n                      module_name=\"{{module_name}}\",\n                      item_names={{item_names}},\n                      move_fields={{move_fields}},\n                      delegate_field=\"{{delegate_field}}\",\n                      callback_externals={{callback_externals}},\n                      rewrite_remaining_accessors={{rewrite_remaining_accessors}},\n                      target_prelude=\"{{target_prelude}}\",\n                      visibility=\"{{visibility}}\",\n                      wiring_mode=<a JSON object chosen from {{wiring_strategy}}: own_construction -> OMIT this param entirely; none -> {strategy: none}; external_injection -> {strategy: external_injection, delegate_field_annotations: [@Inject], delegate_field_modifiers: [private], delegate_field_annotation_imports: [javax.inject.Inject], target_constructor_annotations: [@Inject], target_constructor_annotation_imports: [javax.inject.Inject]}>,\n                      source_delegate_wrappers={{source_delegate_wrappers}},\n                      propagate_class_annotations=\"{{propagate_class_annotations}}\")\n   Inspect the response: captured_variables (with source_mutable +\n   source_static_final flags), external_calls (each entry now carries\n   source_visibility, source_is_static, and recommended_resolution —\n   `cross_class_static_call` means the call site was auto-qualified to\n   `<SourceClass>.<method>(...)` and no FIXME emitted), inherited_dependencies,\n   remaining_source_accessors, remaining_source_constant_refs.\n\n   Import-injection paths (on both source and target) dedupe only against\n   same-package wildcards; foreign wildcards like `import java.util.*;`\n   no longer block unrelated import additions. Record-component\n   rewriting covers both method parameters AND local variables whose\n   explicit declared type matches an inner record. mutable_capture\n   FIXMEs are suppressed under `wiring_strategy=external_injection` since\n   the target is container-constructed and the captures aren't stale.\n   The generated target carries FIXME markers for unresolved externals,\n   inherited calls, mutable captures (own_construction only), and partial\n   implements clauses.\n\n3. Decide:\n   - HARD REFUSAL codes (plan kind returns these directly):\n     - mutable_capture_with_write: extracted body writes to a non-final\n       source field NOT in move_fields. Surface the offending fields in\n       block_reason; the operator must add them to move_fields.\n     - nested_class_in_item_names: an inner-class name was passed.\n       Surface the hint to use java-promote-inner-class first.\n     - method_overload_ambiguous / method_overload_no_match: signature\n       disambiguation failed. Surface the available overloads.\n     - callback_arity_unsupported: a callback_externals method takes 2+\n       args. Surface the offending method.\n   - SOFT REFUSAL (operator decision): if external_calls and\n     inherited_dependencies are non-empty AND the FIXME count exceeds\n     operator tolerance, save the plan via output_path and emit\n     bbox_note(kind=\"blocked\") with the FIXME summary + plan_path.\n   - Otherwise proceed.\n\n4. Apply (if inputs.apply == true) or return plan-only:\n   bbox_refactor_run(dispatch_origin=\"agent\", confirm=true, steps=[\n     <extract_java_class step>,\n     {\"op\":\"command\",\"command\":\"<validation_cmd[0]>\",\"args\":<validation_cmd[1..]>,\"required\":true}\n   ])\n   Default validation_cmd is [\"mvn\", \"compile\"]; pass [\"./gradlew\",\n   \"compileJava\"] or [\"mvn\", \"test\"] depending on the project's build\n   system and validation strictness. extract_java_class does not yet\n   support intra-run rust_compile_fix_round-style repair; the compile\n   step is required=true and rolls back on failure.\n\n   When dispatching apply, pass `cwd` (or `project_dir`) to\n   bbox_refactor_apply so the cross_worktree_apply guard fires correctly.\n   Apply refuses when the caller's git toplevel differs from the plan's\n   recorded paths and `force_path` is not set.\n\n5. Emit done note:\n   bbox_note(kind=\"done\", body=<one-line summary: methods moved, fields\n     moved, FIXMEs by kind (external/inherited/mutable_capture/implements),\n     cross-package status, wiring_strategy, validation result>).\n\nStrict refusal rules (prompt-discipline; not enforced at dispatch):\n- Never call any tool outside the refactor persona tool surface.\n- Never invent method/field names from the user prompt — re-derive from\n  bbox_code_symbols / bbox_refactor_status.\n- Never apply when status=blocked.\n- Never pass an inner-class name in item_names.\n- Never silently drop FIXMEs from the done-note summary — operators need\n  to know what survives unresolved.\n- Never use `wiring_strategy=own_construction` on a source with `@Inject`\n  fields — it null-captures the delegate; choose external_injection or none.\n\nInputs:\n{{project_dir}} {{source}} {{target}} {{module_name}} {{item_names}} {{move_fields}} {{delegate_field}} {{target_prelude}} {{rewrite_remaining_accessors}} {{callback_externals}} {{visibility}} {{wiring_strategy}} {{source_delegate_wrappers}} {{propagate_class_annotations}} {{apply}} {{validation_cmd}}"
    },
    "outputs": {
      "schema": {
        "type": "object",
        "required": [
          "status"
        ],
        "properties": {
          "status": {
            "enum": [
              "planned",
              "applied",
              "blocked",
              "errored"
            ]
          },
          "plan_path": {
            "type": "string"
          },
          "files_touched": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "fixme_count": {
            "type": "object",
            "properties": {
              "plan_only": {
                "type": "integer",
                "minimum": 0
              },
              "warning": {
                "type": "integer",
                "minimum": 0
              },
              "by_kind": {
                "type": "object",
                "properties": {
                  "external_call": {
                    "type": "integer",
                    "minimum": 0
                  },
                  "inherited_call": {
                    "type": "integer",
                    "minimum": 0
                  },
                  "mutable_capture": {
                    "type": "integer",
                    "minimum": 0
                  },
                  "implements_partial": {
                    "type": "integer",
                    "minimum": 0
                  }
                }
              }
            }
          },
          "deep_analysis_summary": {
            "type": "object",
            "additionalProperties": true,
            "description": "captured_variables, external_calls, inherited_dependencies, remaining_source_accessors, remaining_source_constant_refs subsets."
          },
          "cargo_result": {
            "type": "object",
            "description": "Misnamed in the base schema for Java atoms; carries mvn/gradle command + exit_code + summary + rolled_back. The runner output shape is shared across languages.",
            "properties": {
              "command": {
                "type": "string"
              },
              "exit_code": {
                "type": "integer"
              },
              "summary": {
                "type": "string"
              },
              "rolled_back": {
                "type": "boolean"
              }
            }
          },
          "block_reason": {
            "type": "string"
          },
          "done_note_id": {
            "type": "string"
          },
          "methods_moved": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "fields_moved": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "constants_moved": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "delegate_field": {
            "type": "string"
          },
          "target_package": {
            "type": "string"
          },
          "cross_package": {
            "type": "boolean"
          }
        }
      },
      "evidence_density": "high"
    },
    "effects": {
      "writes_files": true,
      "dispatches_runs": 1,
      "max_depth": 0,
      "uses_network": false
    },
    "composition": {
      "may_invoke_atoms": {
        "kind": "none"
      }
    },
    "implementation": {
      "kind": "profile",
      "brofile_ref": "brofile:java-refactor-persona@v1"
    },
    "supervision": {
      "oracle": "default"
    },
    "trace": {
      "retain": "summary",
      "portal_focus": "on_attention"
    },
    "cost_class": "normal",
    "provenance": {
      "kind": "hand_authored",
      "author": "blackbox-refactor-agents",
      "created_at": "2026-05-11T00:00:00Z"
    }
  }
}
