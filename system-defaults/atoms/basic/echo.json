{
  "_contract": "atom/v1",
  "kind": "atom",
  "name": "echo",
  "version": 1,
  "subcontract": "utility/v1",
  "manifest": {
    "description": "Return the invocation arguments unchanged under an echo key. Useful for smoke tests and workflow atom-binding probes.",
    "when_to_use": [
      "checking that atom_invoke is installed and ownership tracking works",
      "testing workflow atom_bindings without provider dispatch",
      "building small examples that need a deterministic atom result"
    ],
    "anti_patterns": [
      "do not use as a substitute for a real adapter or LLM-backed atom",
      "do not use when the caller expects side effects"
    ],
    "inputs": {
      "schema": {
        "type": "object",
        "additionalProperties": true
      }
    },
    "outputs": {
      "schema": {
        "type": "object",
        "required": ["echo"],
        "properties": {
          "echo": {}
        },
        "additionalProperties": false
      },
      "evidence_density": "low"
    },
    "effects": {
      "writes_files": false,
      "dispatches_runs": 0,
      "max_depth": 0,
      "uses_network": false
    },
    "composition": {
      "may_invoke_atoms": { "kind": "none" }
    },
    "implementation": {
      "kind": "deterministic",
      "runner": "echo"
    },
    "trace": {
      "retain": "summary",
      "portal_focus": "on_request"
    },
    "cost_class": "cheap",
    "provenance": {
      "kind": "hand_authored",
      "author": "blackbox-system-defaults",
      "created_at": "2026-05-13T00:00:00Z"
    }
  }
}
