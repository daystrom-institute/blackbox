[package]
name = "bbox-source-graph"
version = "0.0.1"
edition = "2024"

[dependencies]
anyhow = "1"
bbox-corpus-core = { path = "../bbox-corpus-core" }
bbox-project-graph = { path = "../bbox-project-graph" }
hex = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
tempfile = "3"
