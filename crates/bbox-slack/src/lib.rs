//! bbox-slack — extracted from the origin crate by `extract_rust_crate`.
//! Modules move verbatim; the origin re-exports them under their original
//! `crate::<module>` paths.

pub mod slack_channel_bindings;
pub mod slack_proposal_links;
