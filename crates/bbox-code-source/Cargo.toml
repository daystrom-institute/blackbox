[package]
name = "bbox-code-source"
version = "0.0.1"
edition = "2024"

[dependencies]
bbox-corpus-core = { path = "../bbox-corpus-core" }
hex = "0.4"
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
thiserror = "2"

[dev-dependencies]
serde_json = "1"
