//! bbox-knowledge — extracted from the origin crate by `extract_rust_crate`.
//! Modules move verbatim; the origin re-exports them under their original
//! `crate::<module>` paths.

pub mod inventory;
pub mod knowledge;
pub mod overlay;
pub mod render;
pub use bbox_corpus_core::transaction;
