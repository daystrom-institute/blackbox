[package]
name = "bro-apply-patch"
version = "0.0.1"
edition = "2024"

# The codex `*** Begin Patch` edit format: parser + fuzzy-context apply.
# Vendored/adapted from openai/codex (Apache-2.0); see NOTICE. Leaf crate — no
# bro-* dependencies, so it sits at the bottom of the contract DAG.

[lib]
name = "bro_apply_patch"
path = "src/lib.rs"

[dependencies]
thiserror = "2"

[dev-dependencies]
tempfile = "3"
