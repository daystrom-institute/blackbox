# Build-time properties injected by Maven resource filtering.
# The ${rewrite.version} placeholder is replaced during `mvn package`.
# This is the authoritative version source for getCapabilities — more
# reliable than reading from dependency pom.properties in a shaded jar.
openrewrite.version=${rewrite.version}
