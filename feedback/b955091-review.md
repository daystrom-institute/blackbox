Blocker: `composition.fan_out_aggregator` rejects every value outside the three built-ins (`src/orchestration/agents/validate.rs`), but AS-C1 requires enum membership OR a named workflow node id (`design/agent-system-impl.md:612-614`, `design/agent-system.md:447-449`). Since install-time agent validation has no concrete workflow graph to resolve, allow well-formed non-empty workflow-node identifiers as forward/free-form references; reject only empty/whitespace or structurally invalid node ids. Add a positive test for a custom node id and keep a negative test for a malformed value.

Blocker: the reference workflows under `examples/agents/workflows/` demonstrate raw executor brofiles, not hand-authored workflows against `bro_agent_dispatch` as AS-C1 requires (`design/agent-system-impl.md:615-622`). Rewrite `chain.json`, `fan-out.json`, and `escalation.json` to use the workflow `mcp_call` pattern invoking `bro_agent_dispatch` for agent A/B/N/escalation candidates, with downstream nodes consuming the returned session/task outputs according to existing workflow schema conventions.

Issue: add a focused test or validation path that proves the three reference workflow JSON files parse/install as workflow artifacts. The current checkpoint only reports agent validation tests; AS-C1 gate says reference workflows install via `bbox_artifact_install(kind="workflow")`.
