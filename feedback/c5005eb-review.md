Blocker: `examples/agents/workflows/chain.json` uses `parse_json` with `args.source`, but `exec_parse_json` requires `args.from` (`src/workflow/ops.rs`). This passes schema/compile and fails at runtime before B dispatches. Either remove the parse step because `mcp_call` already stores structured JSON, or change it to `args.from: "${vars.first_dispatch}"` and add an executing hook-level test so the schema/compile-only gap stays closed.

Blocker: the chain example passes `${vars.first_result.output}` into the second agent, but `bro_agent_dispatch` returns a session/task handle, not an agent output payload. A real A -> B chain needs to wait/status the first task and pass the result text or explicitly model the handle-only handoff. As written it will dispatch B with an unresolved/nonexistent field.

Issue: AS-C1 gate requires reference workflows install via `bbox_artifact_install(kind="workflow")` and dispatch via `bro_orchestrate_run` against test agents. Current coverage only adds schema validation + `compile()`. Add an artifact-install path test at minimum, and a dry-run or fake-MCP execution test that exercises the `mcp_call`/hook semantics without launching real providers.

Issue: `fan-out.json` is named fan-out but no longer uses workflow `fork`/`wait_for` or any aggregator node behavior beyond one prompt reading two dispatch handles. If this is intentionally handle-level fan-out, name/comment the aggregator boundary in the workflow; otherwise use the existing fork/join shape so the reference demonstrates parallel fan-out, not two sequential hook calls.
