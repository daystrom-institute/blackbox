Blocker: AS-IaC1 tests call `validate_agent_install` with fake `brofile_exists`/adapter state, but the gate requires each reference artifact install cleanly through `bbox_artifact_install`. Add a full install-path test that installs `examples/agents/code-reviewer-persona.json` as `kind=brofile`, then installs `code-reviewer.json`, `diff-narrator.json`, and `badgey.json` as `kind=agent` through `install_artifact_value`/the same dispatcher used by `bbox_artifact_install`.

Blocker: `examples/agents/code-reviewer.json` uses `{{#each context_refs}}...{{/each}}`, but `BlackboxServer::expand_template` only replaces simple `{{key}}` tokens. Dispatching the example with valid args will leak the loop syntax into the prompt. Either simplify the template to supported placeholders or implement/test the documented loop behavior before shipping this as a reference manifest.

Issue: AS-IaC1 gate says each reference agent is dispatchable end-to-end via `bro_agent_dispatch` with valid args. Current tests stop at install validation and do not dispatch the bundled agents. Add at least one cheap/noop-adapter or spawn-stub path that proves `bro_agent_dispatch` returns the expected handle for the reference agents without launching real providers.

Issue: `badgey.json` is marked as `dispatch_adapter: "badgey"` and also carries a full inline brofile. If the adapter is meant to own badgey lifecycle entirely, make the placeholder contract explicit in the manifest or test; otherwise future readers will assume the inline brofile direct path matters even though adapter dispatch bypasses it.
