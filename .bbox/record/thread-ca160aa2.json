{
  "id": "thread-ca160aa2",
  "topic": "bro-harness remaining work",
  "status": "promoted",
  "kind": "work_item",
  "promoted_to": "design/bro-harness/backlog-transport-polish.md",
  "notes": [
    "Remaining work for the custom provider harness (crates/bro-harness, crates/bro-tools; branch feat/bro-harness). Design: design/orchestration/anthropic-harness.md.\n\nDone so far (committed): 3-transport harness (anthropic/openai-chat/openai-responses) behind a Transport trait; ChatGPT-OAuth refresh; rmcp MCP client; workspace+web tool bodies; client-side deferred-tooling with Pinned/Eager/Deferred tiers + PinPolicy; daemon wiring for glm/deepseek (anthropic transport) + new `brodex` provider (openai-responses); reasoning-item replay fix; removal of the surface→client-filter entanglement from bro_exec/resume (surface is server-side URL-only).\n\nRemaining (this thread):\n1. allow/deny lists in bro-harness (LEGIT client plane, orthogonal to surface). build_filter_args is a no-op for glm/deepseek/brodex because the harness CLI doesn't accept claude's --allowedTools/--disallowedTools. So harness providers currently get NO client-side recursion guard or per-dispatch allow/disallow. Add a harness-native --deny-tools/--allow-tools flag (qualified mcp__server__tool patterns), have build_filter_args emit it for harness providers, and enforce in the harness registry (denied MCP tools excluded entirely: not listed, not tool_search-loadable, dispatch-refused). Test: recursion guard denies bro_* for a brodex/glm dispatch. NOTE: this is allow/deny, NOT surface — do not reintroduce surface awareness.\n2. SSE streaming for anthropic + openai-chat transports (responses already parses SSE). Emit token-by-token stream_event deltas instead of the non-streaming assistant+result fallback.\n3. Validate session transcript emission + resume: wire-contract test capturing harness stdout and running the daemon's parse_claude_event over it; confirm --resume round-trips via the transport snapshot store.\n4. Robustness policies for LLM API calls: transient-error retry with capped backoff, request timeouts, rate-limit (429/Retry-After) handling across all three transport clients (mirror pg_recon ClaudeChatClient).\n5. Tool stubs / polish: smart_read is heuristic-only; optional client-side web_search fallback (Brave) for providers without a server-side tool; MCP connection pooling (currently per-call dial); deferred-manifest token trimming.\n\nAuthorized (operator, overnight): LLM spend + daemon restarts for live e2e via bro_exec. Commit at milestones.",
    "Overnight autonomous progress (session c2ed178f). Commits on feat/bro-harness:\n\nDONE + live-verified (prod daemon restarted, real bro_exec/bro_resume):\n- c2653db3 — removed the surface→client-filter entanglement from bro_exec/resume (Chesterton's-fence-checked: the fence reason \"provider/daemon agree\" was redundant since server-side list_tools already scopes the provider's universe; surface stays server-side via the ?surface= URL). resolve_dispatch_filters no longer takes surface/packets; surface_to_filters + ExecParams/ResumeParams.surface gone.\n- d929f655 — client-side allow/deny in harness (item 1). build_filter_args emits --deny-tools/--allow-tools (comma, qualified mcp__server__tool) for glm/deepseek/brodex; harness ToolFilter drops denied MCP tools at load. Live: recursion guard verified, admitted=114 denied=27 (bro_* dropped).\n- 56ba0749 — HTTP robustness (item 4). transport/http::send_with_retry: retry on connect/timeout/408/425/429/5xx with capped backoff + Retry-After, per-request timeout; wired into all 3 transports.\n- 7836b13c — resume fix + wire-contract test (item 3). Found+fixed: resume failed because the daemon doesn't re-pass --model on resume and glm env has no ANTHROPIC_MODEL; harness now persists model in the session snapshot and falls back to it. Live: glm exec→resume round-trips (\"PLUM-88\" recalled). Added events.rs test feeding the harness's exact stream-json envelope through parse_claude_event.\n\nREMAINING (deferred to a fresh session — not started, to avoid a rushed streaming refactor at the end of a long session):\n- item 2: SSE streaming for anthropic + openai-chat (responses already parses SSE). Change stream:false→true, parse content_block_delta (anthropic) / choices[].delta (openai-chat), emit token-by-token stream_event text deltas via emit.rs. Non-streaming currently works and is wire-compatible, so this is latency/UX, not correctness.\n- item 5 polish: smart_read is heuristic-only (works); optional client-side web_search (Brave) fallback for providers lacking a server tool; MCP connection pooling (per-call dial today); deferred-manifest token trimming; wrap codex_auth token refresh in the retry helper.\n\nState: all crates build, clippy clean, harness 10 tests + daemon surface/dispatch/mcp/provider tests green. Prod daemon (:7264) running the new blackboxd + bro-harness.",
    "Item-5 residue captured into a proposed design doc as part of the bro-harness docs reorg: design/bro-harness/backlog-transport-polish.md (lifecycle: proposed). Covers MCP connection pooling (mcp.rs:155 re-dials per call), codex_auth retry-helper wrapping, deferred-manifest token trimming, plus the anthropic-harness \"open questions\" (web_search/Brave fallback, result normalization, structured output, namespace iso, in-process executor, RTK output compaction). Items 1-4 + item 2 (SSE) are noted as DONE in the doc. bro-harness is now a top-level design home (design/bro-harness/) with a nav hub at design/bro-harness/bro-harness.md sorting shipped as-built records vs proposed backlog. Thread stays open as the work tracker.",
    "NARROWED SCOPE. Done + landed (drop from tracking): item 1 surface/client-filter split, item 1 allow/deny recursion guard, item 3 resume + wire-contract test, item 4 HTTP robustness (retry/backoff/Retry-After), item 2 SSE streaming across all three transports. REMAINING (the only open work) = item-5 polish: MCP connection pooling (mcp.rs re-dials per call), codex_auth refresh not wrapped in the http retry helper, optional client-side web_search (Brave) fallback, deferred-manifest token trimming. Full pickup spec lives in design/bro-harness/backlog-transport-polish.md.",
    "Tracking handed off to the committed, repo-owned backlog doc design/bro-harness/backlog-transport-polish.md (lifecycle: proposed). Remaining = item-5 polish only (pooling, codex_auth retry, Brave fallback, manifest trim); items 1-4 + SSE shipped. The doc is now the system of record."
  ],
  "created_at": "2026-05-29T08:21:13Z",
  "resolved_at": "2026-05-31T16:42:57Z"
}